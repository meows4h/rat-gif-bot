import random
import math
import eureka
import datetime as date_time
import discord
from datetime import timedelta

def get_time():

    timestamp = '' # write function that finds the time right now, specifically the date as it relates to PST
    
    return timestamp


def get_response(msg_data, message: str) -> str:
    p_message = message.lower()
    
    if p_message == '!rat':

        rat_gif = '' # roll random number, choose random rat gif

        return rat_gif

    if p_message == '!randrat':

        # roll number, check if it matches today date, win if does, lose if not
        rat gif = ''

        return rat_gif